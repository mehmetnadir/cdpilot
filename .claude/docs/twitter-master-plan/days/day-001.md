# Day 001 — T1 Foundations

## Topic
CDP WebSocket session init

## Tweet 1/5 — Hook
Most devs think "automation" and think Selenium or Playwright. But they're just wrappers for wrappers. Underneath is a WebSocket talking JSON-RPC to a browser process. It's raw, it's fast, and it's how things actually work. Ever looked at the raw frames?

## Tweet 2/5
WebDriver was designed for cross-browser testing in 2011. CDP (Chrome DevTools Protocol) was designed to build DevTools. One is for compliance, the other is for deep surgical control of the engine. These are different tools for different problems.

## Tweet 3/5
When you connect, you send a JSON message: `{"id":1,"method":"Target.getTargets"}`. The browser responds with a list of tabs. No driver binary to download. No binary version mismatch. No Java middleman sitting in the middle.

## Tweet 4/5
cdpilot connects directly to the WebSocket URL. This removes the latency of a driver server and gets us events the second they happen in the renderer. Poll vs push — it's not even close.

## Tweet 5/5 — Anchor
Most automation tools hide this from you. We don't.

## Visual prompt
"Dark terminal screen with glowing green JSON lines streaming over a WebSocket connection diagram, matrix aesthetic, 4K"

## Final tweet
Source: https://github.com/nadirabbas/cdpilot | Full CDP protocol docs: https://chromedevtools.github.io/devtools-protocol/

## Metadata
- track: T1 Foundations
- format: thread
- difficulty: beginner
- target_engagement: question-bait
- hashtags: #devtools
- grok_mention: false
